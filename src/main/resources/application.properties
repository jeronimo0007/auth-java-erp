spring.application.name=auth

# Configuração da porta do servidor
server.port=8080

# Configuração do banco de dados MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://195.200.1.59:3306/app_omny?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:api}
spring.datasource.password=${DB_PASSWORD:c6m8n4d2d3}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuração JWT
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
jwt.expiration=${JWT_EXPIRATION:86400}

# DigitalOcean Spaces (S3 Compatible)
storage.s3.endpoint=https://sfo3.digitaloceanspaces.com
storage.s3.region=sf03
storage.s3.bucket=${STORAGE_S3_NAME_SPACE}
storage.s3.public_base=https://${STORAGE_S3_NAME_SPACE}.sfo3.digitaloceanspaces.com
storage.s3.access_key=${STORAGE_S3_ACCESS_KEY}
storage.s3.secret_key=${STORAGE_S3_SECRET_KEY}

# RabbitMQ Configuration
spring.rabbitmq.host=${RABBITMQ_HOST:72.60.246.107}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
spring.rabbitmq.virtual-host=${RABBITMQ_VHOST:/}

# Queue Configuration
mq.queue.site-creation=site.creation.queue
mq.exchange.site-creation=site.creation.exchange
mq.routing-key.site-creation=site.creation

# Multipart upload limits
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=250MB
# Some environments support this property; if unsupported it's ignored

# Defer multipart parsing so exceptions can be handled by @ControllerAdvice
spring.servlet.multipart.resolve-lazily=true

# App-level upload validation (enforced no matter infra limits)
app.upload.max-file-bytes=${UPLOAD_MAX_FILE_BYTES:5242880}
app.upload.max-file-count=${UPLOAD_MAX_FILE_COUNT:20}
app.upload.allowed-types=${UPLOAD_ALLOWED_TYPES:image/jpeg,image/png,image/webp,application/pdf}

# Site creation limits
app.site.max-sites-per-client=${MAX_SITES_PER_CLIENT:3}
